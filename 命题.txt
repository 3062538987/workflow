命题：
1、配置comfyUI“文生图”工作流（通过GPT/Codex本地搭，可用liblib简单验证后再本地搭

需要至少包含：
（1）正向输入/负向输入、
（2）生图模型
（3）LoRA（约束图像风格）
（4）输出预览

2、生成适用于印花家纺的设计元素
参考发给你的提示词（目前的提示词时Midjourney的，你需要转化成ComfyUI适用的提示词），然后调整【模型】和【LoRA】使生成图片的感觉和参考图感觉相似（因为是家纺元素，不能是照片感的）

注：工作流，即要能够做到批量生成

批量输入，批量输出
